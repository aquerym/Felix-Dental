/** Set to true when the Key Advantages section should appear on the landing page. */
export const showKeyAdvantagesSection = true;

/** Set to true when the Troubleshooting (pricing nav target) section should appear on the landing page. */
export const showTroubleshootingSection = true;
